{"problemName":"time-needed-to-inform-all-employees","language":"cpp","code":"class Solution {\npublic:\n    int dfs(vector<vector<int>> &adj, int curr, vector<int>  &arr){\n        int ans = 0;\n        for(auto i : adj[curr]){\n            ans = max(ans, arr[curr] + dfs(adj, i, arr));\n        }\n        return ans;\n    }\n    int numOfMinutes(int n, int headID, vector<int>& manager, vector<int>& informTime) {\n        vector<vector<int>> adj(n);\n        for(int i = 0; i < n; i++){\n            if(i == headID)continue;\n            adj[manager[i]].push_back(i);\n        }\n        return dfs(adj, headID, informTime);\n    }\n};"}