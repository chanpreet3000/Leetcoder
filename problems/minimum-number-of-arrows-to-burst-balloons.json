{"problemName":"minimum-number-of-arrows-to-burst-balloons","language":"cpp","code":"class Solution {\npublic:\n\n    static bool comp(vector<int> &a, vector<int> &b) {\n        return (a[0] == b[0] ? a[1] < b[1] : a[0] < b[0]);\n    }\n    int findMinArrowShots(vector<vector<int>>& points) {\n        sort(points.begin(), points.end(), comp);\n\n        int ans = 0;\n        int s = points[0][0];\n        int e = points[0][1];\n\n        for(int i = 1; i < points.size(); i++) {\n            if(s < points[i][0] and e < points[i][0]) {\n                ans++;\n                s = points[i][0];\n                e = points[i][1];\n            }\n            else {\n                s = max(s, points[i][0]);\n                e = min(e, points[i][1]);\n            }\n        }\n\n        return ans + 1;\n    }\n};"}