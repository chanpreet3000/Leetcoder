{"problemName":"number-of-visible-people-in-a-queue","language":"cpp","code":"class Solution {\npublic:\n    vector<int> canSeePersonsCount(vector<int>& heights) {\n        int n = heights.size();\n        vector<int> ans(n, 0);\n        stack<int> st;\n        for(int i = n - 1; i >= 0; i--){\n            int num = 0;\n            while(!st.empty() && heights[st.top()] < heights[i]){\n                st.pop();\n                num++;\n            }\n            if(!st.empty())num++;\n            ans[i] = num;\n            st.push(i);\n        }\n        return ans;\n    }\n};"}