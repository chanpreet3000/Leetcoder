{"problemName":"minimum-index-sum-of-two-lists","language":"cpp","code":"class Solution {\npublic:\n    vector<string> findRestaurant(vector<string>& list1, vector<string>& list2) {\n        map<string, int> m;\n        for(int i = 0; i < list2.size();i++){\n            m[list2[i]] = i;\n        }\n        vector<string> ans;\n        int len = 1e9;\n        for(int i = 0; i < list1.size();i++){\n            if(m.find(list1[i]) != m.end()){\n                if(i + m[list1[i]] < len){\n                    ans.clear();\n                    ans.push_back(list1[i]);\n                }else if(i + m[list1[i]] == len){\n                    ans.push_back(list1[i]);\n                }\n                len = min(len, i + m[list1[i]]);\n            }\n        }\n        return ans;\n    }\n};"}