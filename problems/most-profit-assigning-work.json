{"problemName":"most-profit-assigning-work","language":"cpp","code":"class Solution {\npublic:\n    int maxProfitAssignment(vector<int>& difficulty, vector<int>& profit, vector<int>& worker) {\n        vector<int> arr(1e5 + 5, 0);\n        for(int i = 0; i < profit.size(); i++){\n            if(arr[difficulty[i]]/(difficulty[i] * 1.0) < (profit[i])/(difficulty[i] * 1.0)){\n                arr[difficulty[i]] = profit[i];\n            }\n        }\n        for(int i = 1; i < 1e5 + 5; i++){\n            arr[i] = max(arr[i], arr[i - 1]);\n        }\n        long ans = 0;\n        for(auto i : worker){\n            ans += arr[i];\n        }\n        return ans;\n    }\n};"}