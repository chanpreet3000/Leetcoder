{"problemName":"shortest-distance-to-a-character","language":"cpp","code":"class Solution {\npublic:\n    vector<int> shortestToChar(string s, char c) {\n        int n = s.length();\n        vector<int> ans(n, INT_MAX);\n        \n        int prev = -1e5;\n        for(int i = 0; i < n;i++){\n            if(s[i] == c){\n                prev = i;\n            }\n            ans[i] = min(ans[i], i - prev);\n        }\n        prev = 1e5;\n        for(int i = n - 1; i >=0;i--){\n            if(s[i] == c){\n                prev = i;\n            }\n            ans[i] = min(ans[i], prev - i);\n        }\n        return ans;\n    }\n};"}