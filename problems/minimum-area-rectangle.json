{"problemName":"minimum-area-rectangle","language":"cpp","code":"class Solution {\npublic:\n    int minAreaRect(vector<vector<int>>& points) {\n        set<pair<int, int> > pts;\n        for(vector<int> p : points){\n            pts.insert({p[0], p[1]});\n        }\n        int mn = 1e9;\n        for(auto p : pts){\n            for(auto q : pts){\n                if(p.first == q.first || p.second == q.second) continue;\n                if(pts.count({p.first, q.second}) && pts.count({q.first, p.second})){\n                    mn = min(mn, abs((p.first - q.first) * (p.second - q.second)));\n                }\n            }\n        }\n        if(mn == 1e9) return 0;\n        return mn;\n    }\n};"}