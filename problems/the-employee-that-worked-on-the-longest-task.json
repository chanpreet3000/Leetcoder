{"problemName":"the-employee-that-worked-on-the-longest-task","language":"cpp","code":"class Solution {\npublic:\n    static bool cmp(vector<int>&x,vector<int> &y){\n        if(x[1]==y[1]) return x[0]<y[0];\n        return x[1]<y[1];\n    }\n    static bool cmp1(pair<int,int>&x,pair<int,int> &y){\n        if(x.second==y.second) return x.first<y.first;\n        return x.second>y.second;\n    }\n    int hardestWorker(int n, vector<vector<int>>& vec) {\n        sort(begin(vec),end(vec),cmp);\n        int t=0;\n        vector<pair<int,int>> p;\n        for(int i=0;i<vec.size();i++){\n            p.push_back({vec[i][0],vec[i][1]-t});\n            t=vec[i][1];\n        }\n        sort(begin(p),end(p),cmp1);\n        return p[0].first;\n    }\n};"}