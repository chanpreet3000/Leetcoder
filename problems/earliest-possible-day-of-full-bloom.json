{"problemName":"earliest-possible-day-of-full-bloom","language":"cpp","code":"class Solution {\npublic:\n    int earliestFullBloom(vector<int>& plantTime, vector<int>& growTime) {\n        vector<pair<int, int>> arr;\n        for(int i = 0; i < plantTime.size(); i++){\n            arr.push_back({plantTime[i], growTime[i]});\n        }\n        sort(arr.begin(), arr.end(), [&](pair<int, int> &a, pair<int, int> &b){\n            int x = a.first + max(a.second, b.first + b.second);\n            int y = b.first + max(b.second, a.first + a.second);\n            return x < y;\n        });\n        int _max = -1, prev = 0;\n        for(int i = 0; i < plantTime.size(); i++){\n            prev += arr[i].first;\n            _max = max(_max, prev + arr[i].second);\n        }\n        return _max;\n    }\n};"}