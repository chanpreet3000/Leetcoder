{"problemName":"extra-characters-in-a-string","language":"python","code":"def helper(s, mp, ind, dp):\n    if ind == len(s):\n        return 0\n    if dp[ind] != -1:\n        return dp[ind]\n    ans = 1e9\n    t = \"\"\n    for i in range(ind, len(s)):\n        t += s[i]\n        if(t in mp):\n            ans = min(ans, helper(s, mp, i + 1, dp))\n        else:\n            ans = min(ans, i - ind + 1 + helper(s, mp, i + 1, dp))\n    dp[ind] = ans\n    return dp[ind]\n\nclass Solution(object):\n    def minExtraChar(self, s, dictionary):\n        mp = set()\n        dp = [-1] * 51\n        for i in dictionary:\n            mp.add(i)\n        return helper(s, mp, 0, dp)\n    \n  \n"}