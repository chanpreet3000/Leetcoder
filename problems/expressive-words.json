{
    "problemName": "expressive-words",
    "language": "cpp",
    "code": "class Solution {\npublic:\n    int expressiveWords(string s, vector<string>& words) {\n        vector<char> c;\n        vector<int> cnt;\n        work(s, c, cnt);\n        int out = 0;\n        for (int i=0; i<words.size(); ++i){\n            vector<char> tmp_c;\n            vector<int> tmp_cnt;\n            work(words[i], tmp_c, tmp_cnt);\n            if (tmp_c.size() != c.size()) continue;\n            bool good = true;\n            for (int i=0; i<c.size(); ++i){\n                if (tmp_c[i] != c[i] || tmp_cnt[i] > cnt[i] || (tmp_cnt[i] != cnt[i] && cnt[i] < 3)) {good = false; break;}\n            }\n            if (good) out++;\n        }\n        return out;\n    }\n    void work(string& s, vector<char>& c, vector<int>& cnt){\n        int tmp_cnt = 0; \n        for (int i=0; i<s.size(); ++i){\n            /*if (i!=0 && s[i] != s[i-1]){\n                if (!comp) {c.push_back(s[i-1]); cnt.push_back(tmp_cnt);}\n                else if (id >= c.size() || c[id] != s[i-1] || cnt[id] > tmp_cnt || (cnt[id]!=tmp_cnt && tmp_cnt < 3)  ) return false;\n                tmp = 1;\n                id++;\n            } else tmp++;*/\n            if (i!=0 && s[i] != s[i-1]) {c.push_back(s[i-1]); cnt.push_back(tmp_cnt); tmp_cnt = 1;}\n            else tmp_cnt ++;\n        }\n        c.push_back(s.back()); cnt.push_back(tmp_cnt); \n        /*cout << s << \"~~\" << endl;\n        for (char cc : c) cout << cc << \"###\";\n        cout << endl;\n        for (int e : cnt) cout << e << \"###\";\n        cout << endl;*/\n    }\n};"
}