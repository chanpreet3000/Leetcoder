{"problemName":"permutations-ii","language":"cpp","code":"class Solution {\npublic:\n    void helper(vector<int> & nums, set<vector<int>> &ans, int ind){\n        if(ind == nums.size()){\n            ans.insert(nums);\n            return;\n        }\n        for(int i=ind;i<nums.size();i++){\n            swap(nums[ind], nums[i]);\n            helper(nums, ans, ind + 1);\n            swap(nums[ind], nums[i]);\n        }\n    }\n    vector<vector<int>> permuteUnique(vector<int>& nums) {\n        set<vector<int>> st;\n        sort(nums.begin(), nums.end());\n        helper(nums, st, 0);\n        vector<vector<int>> ans;\n        for(auto i : st) ans.push_back(i);\n        return ans;\n    }\n};"}