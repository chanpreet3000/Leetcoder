{"problemName":"mean-of-array-after-removing-some-elements","language":"cpp","code":"class Solution {\npublic:\n    double trimMean(vector<int>& arr) {\n        sort(arr.begin(), arr.end());\n        double sum = 0;\n        for(int i = arr.size()/20; i < arr.size() - arr.size()/20;i++){\n            sum += arr[i];\n        }\n        return sum/(arr.size() - arr.size()/10);\n    }\n};"}