{"problemName":"widest-vertical-area-between-two-points-containing-no-points","language":"cpp","code":"class Solution {\npublic:\n    int maxWidthOfVerticalArea(vector<vector<int>>& points) {\n        \n        sort(points.begin(), points.end());\n    \n        int ans = 0;\n        for(int i = 1; i < points.size(); i++) {\n            int dist = points[i][0] - points[i - 1][0];\n            ans = max(ans, dist);\n        }\n        return ans;\n        \n       \n    }\n};"}