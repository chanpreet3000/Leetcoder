{"problemName":"relative-ranks","language":"cpp","code":"class Solution {\npublic:\n    vector<string> findRelativeRanks(vector<int>& score) {\n        vector<pair<int, int>> arr;\n        for(int i = 0; i < score.size();i++){\n            arr.push_back({score[i], i});\n        }\n        sort(arr.begin(), arr.end());\n        map<int, string> m;\n        m[0] = \"Gold Medal\";\n        m[1] = \"Silver Medal\";\n        m[2] = \"Bronze Medal\";\n        vector<string> ans(score.size());\n        for(int i = arr.size() - 1; i>=0;i--){\n            auto p = arr[i];\n            if(m.find(arr.size() - 1 - i) != m.end()){\n                ans[p.second] = m[arr.size() - 1 - i];\n            }else{\n                ans[p.second] = to_string(arr.size() - i);\n            }\n        }\n        return ans;\n    }\n};"}